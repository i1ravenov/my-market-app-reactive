services:

  payment-service:
    build:
      context: .
      dockerfile: payment-service/Dockerfile
    ports:
      - "8081:8081"

  app:
    depends_on:
      - payment-service
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8080:8080"
